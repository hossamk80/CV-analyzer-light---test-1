# دليل التثبيت والتشغيل خطوة بخطوة — Smart Recruitment Suite

دليل تشغيل ذاتي الاستضافة لنظام تحليل ومطابقة السير الذاتية بالذكاء الاصطناعي.

---

## المتطلبات الأساسية
1. **Node.js**: إصدار 18 أو أحدث (يدعم النظام إصدار Node 24 الحالي).
2. **منفذ 3000**: يجب أن يكون منفذ الشبكة 3000 غير مستخدم من تطبيق آخر (أو قم بتغييره في ملف `.env.local`).
3. **مفتاح Google Gemini API**: (اختياري للتشغيل المحلي، ولكنه إلزامي لبدء عمليات تحليل السير الذاتية). يمكنك الحصول عليه مجاناً من Google AI Studio.

---

## خطوات التثبيت المحلي والتطوير

### 1. تثبيت الاعتماديات
افتح نافذة أمر (Terminal/Command Prompt) في المجلد الرئيسي للمشروع ونفذ:
```bash
# استخدام pnpm (موصى به)
pnpm install

# أو استخدام npm العادي
npm install
```

### 2. إعداد ملفات البيئة والتكوين
قم بنسخ ملف الإعداد التجريبي `.env.example` إلى ملف محلي باسم `.env.local`:
```bash
cp .env.example .env.local
```
افتح ملف `.env.local` وقم بتهيئة المتغيرات:
* `GEMINI_API_KEY`: ضع مفتاح Gemini الخاص بك هنا.
* `AUTH_SECRET`: ضع رمزاً عشوائياً طويلاً لتأمين رموز الجلسات (JWT).

### 3. التشغيل في وضع التطوير (Development Mode)
قم بتشغيل خادم التطوير (الذي يدمج خادم Express مع Vite تلقائياً):
```bash
# استخدام pnpm
pnpm run dev

# أو استخدام سكربت ويندوز الجاهز
.\scripts\run.bat

# أو سكربت لينكس/ماك الجاهز
./scripts/run.sh
```
افتح المتصفح على الرابط التالي: [http://localhost:3000](http://localhost:3000)

---

## النشر الفعلي للإنتاج (Production Deployment)

لتشغيل النظام بأقصى أداء مع ضغط ودمج ملفات الواجهة الأمامية والخلفية:

### 1. بناء حزم الإنتاج
نفذ أمر البناء:
```bash
pnpm run build
```
سيقوم هذا الأمر ببناء ملفات الواجهة في مجلد `dist/` وتجميع خادم backend في ملف مستقل `dist/server.cjs`.

### 2. تشغيل خادم الإنتاج
لتشغيل الخادم مباشرة:
```bash
pnpm start
```
أو استخدم السكربتات الجاهزة:
* على ويندوز: `.\scripts\start-prod.bat`
* على لينكس/ماك: `./scripts/start-prod.sh`

### 3. التشغيل الدائم 24/7 باستخدام PM2
لضمان استمرار عمل الخادم في الخلفية وإعادة تشغيله تلقائياً عند إقلاع نظام التشغيل:
```bash
# تثبيت PM2 عالمياً
npm install -g pm2

# تشغيل التطبيق باستخدام ملف التهيئة المرفق
pm2 start ecosystem.config.cjs

# حفظ إعدادات التشغيل التلقائي
pm2 save
pm2 startup
```

---

## حسابات الدخول التجريبية الافتراضية
تُهيّأ قاعدة بيانات النظام تلقائياً عند أول تشغيل وتُزرع فيها الحسابات التالية (يرجى تغيير كلمات المرور من إعدادات الحساب فور الدخول في بيئة حقيقية):

| اسم المستخدم | كلمة المرور | الدور الوظيفي (Role) |
|--------------|-------------|-------------------|
| `admin`      | `admin123`  | مدير النظام (Admin) |
| `manager`    | `manager123`| مدير التوظيف (Hiring Manager) |
| `recruiter`  | `recruiter123`| مسؤول التوظيف (Recruiter) |

---

## النسخ الاحتياطي
النظام مصمم ليعمل بالكامل محلياً. جميع البيانات (الوظائف، المرشحين، نتائج التقييم، الإعدادات، البرومبتات) تُخزن في ملف قاعدة بيانات محلي واحد باسم `sqlite.db` في مجلد المشروع الرئيسي.
* لأخذ نسخة احتياطية: قم بنسخ ملف `sqlite.db` فقط وضعه في مكان آمن.
* لاستعادة البيانات: قم بإعادة ملف `sqlite.db` إلى مكانه الأصلي قبل تشغيل الخادم.
